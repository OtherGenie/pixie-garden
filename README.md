# Pixie's Garden 🌸

Click (or tap) anywhere and a flower grows. Built with [Three.js](https://threejs.org/) + Vite.

## Run it

```bash
npm install
npm run dev
```

Then open the URL it prints (usually http://localhost:5173).
